# Results

> Removed: this file used to hold the auto-generated segmentation evaluation
> table (`scripts/evaluate.py` + `docs/results/*.json`). The build is now
> YOLO-only; see `docs/yolo_mvp.md` and the YOLO training outputs under
> workspace-root `results/` for detection performance.
