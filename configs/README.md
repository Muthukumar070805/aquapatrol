# configs

This directory held YAML training-run configurations for the removed
segmentation pipeline. It is kept as an (empty) skeleton; detection is now
YOLO-only and configured via `OILSPILL_API_*` environment variables (see
`.env.example` and `docs/yolo_mvp.md`).
